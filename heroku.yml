build:
  docker:
    web: Dockerfile

run:
  web: /bin/bash ./heroku-docker-entrypoint.sh
# release:
#   image: web
#   command:
#     - python manage.py collectstatic --no-input
#     - python manage.py makemigrations
#     - python manage.py migrate
